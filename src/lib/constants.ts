/**
 * Incrementá este número cada vez que reemplaces logo.png
 * para que el navegador y Next.js carguen la nueva imagen.
 */
export const LOGO_VERSION = 2;

export const logoSrc = `/logo.png?v=${LOGO_VERSION}`;
